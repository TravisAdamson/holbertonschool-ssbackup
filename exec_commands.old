#include "shellshocked.h"

int execute_command(const char *command, char *envp[]) {
    char *command_path;
    char *path_token;
    pid_t pid;
    int status;
    struct stat st;

    pid = fork();
    if (pid == -1) {
        perror("fork");
        return 1;
    }

    if (pid == 0) {
        char *args[MAX_COMMAND_LENGTH];
        int i = 0;
